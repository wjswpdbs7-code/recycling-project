# 진행 일지 — 가정용 분리수거 자동분류기

> 목적: 사용자와 AI가 프로젝트 흐름을 빠르게 파악하기 위한 일자별 기록.
> 규칙: 의미 있는 작업·결정이 있을 때마다 그 날짜에 추가한다. 형식은 "한 일 / 결정 / 산출물" 3줄 구조를 기본으로.

---

## 2026-07-15 (화)

- **한 일**: 프로젝트 설계서 v0.1 작성 (6클래스, 캐러셀 방식, 저가형/표준형 티어). Pico 2 W + 서보 2개 + 버튼 테스트 (MicroPython)
- **결정**: 분배 기구는 회전 캐러셀 방식 채택
- **산출물**: `docs/01_설계서.md`(당시 루트), `servo_button_test.py`

## 2026-07-16 (수)

- **한 일**:
  - 서보+버튼 테스트를 C(Pico SDK)로 변환, WSL에 빌드 환경 구축(ARM 툴체인·SDK, sudo 없이) → `servo_button.uf2` 빌드 성공
  - 언어 전략 확정: Python(AI·서비스·대시보드) + C(MCU 실시간 제어)
  - 아파트 규모 논의 → 벌크 싱귤레이션은 선별장 영역, 투입구 제어(RVM 방식)가 정답. 처리량 계산
  - **클래스 4종 축소 결정** (비닐·종이 제외 → 경질 용기류만): 난이도가 "도전적→검증된 영역"으로
  - 데이터셋 조사(AI Hub 생활폐기물 71385 추천), 사용자가 받은 샘플 105장 분석 — 실내형분류기 파트가 우리 구도와 일치 확인
  - 학습 비용·시간 추정($10~20), 성능 목표 3층(모델/실기기/시스템) 정의
  - **RunPod 파이프라인 검증**: JSON→YOLO 변환기 작성, A40 팟에서 샘플 105장 스모크 학습(mAP50 0.911) 후 팟 종료 — 총 $0.11
  - 아파트 분리배출 규정 조사(투명페트 의무 등), YOLO 필요성 확정(검출=개수 검증 겸용), MCU/NPU 엣지 사양 조사(STM32N6 34FPS 실증), 혼합 쓰레기 싱귤레이터 기구 설계(클리트 컨베이어+리턴 루프, BOM 9~17만원)
- **결정**: 4클래스(투명페트/플라스틱/캔/유리) + 판별불가(비학습·후처리), YOLO11n, RunPod A40급, 실내형분류기 파트만 사용
- **산출물**: `servo_button_c/`, `training/convert_aihub_to_yolo.py`, `models`(스모크 테스트 결과는 `training/results_pipeline_test/`)

## 2026-07-17 (목)

- **한 일**:
  - AI Hub 공식 가이드라인 PDF(53쪽) 분석 → **29종 클래스 코드 공식 매핑표 확보** (페트=투명 무색만 확인)
  - aihubshell + API 키로 B2 파트(86MB) 테스트 다운로드 → 실측: B2는 93%가 오염 배치 → 오염 클래스 필요성 부상
  - 오염 라벨 기준 실물 확인(경계 애매성 발견), 데이터셋 분리 구조(품목×상태 매트릭스) 분석
  - **클래스 설계 탐구 → 9클래스(4품목+품목별오염4+other) 채택** — B2 실측 활용률 4%(4클래스) vs 100%(9클래스). `docs/02_클래스_설계.md` 작성
  - Run A 준비: A40 팟 생성 → **AI Hub 해외 IP 차단 발견** → 국내 로컬 다운로드(16GB) 후 rsync 업로드로 우회 (팟에 rsync 설치 필요했음)
  - 공식 Validation 실내형분류기 12,784장 로컬 확보·분포 분석 (학습 미사용, 최종 평가 전용 원칙 수립)
  - **Run A(4클래스 기준선) 학습 시작** — 실분포 확정: 39,643장/102,271장(38%). 중간 과적합 점검: 이상 없음
- **결정**: 9클래스 설계(당시), 평가는 공식 Validation + B폴더(조명)별 분해, Run B는 배치·worker 최적화 적용
- **산출물**: `docs/02_클래스_설계.md`, `training/aihub_download/`(Training 16GB + Validation 2GB), 변환기 모드 4/8/9

## 2026-07-18 (금)

- **한 일**:
  - **Run A 완료** (80ep, 5.2h): 공식 Validation **mAP50 0.985** (페트 0.991/플라스틱 0.962/캔 0.994/유리 0.991) — 전 클래스 목표(0.95) 통과, 과적합 없음
  - 모델 회수: `models/runA_yolo11n_4cls_best.pt`
  - **최종 클래스 17종으로 상향 확정** (아파트 규정 풀스펙: 클린9+오염8, other 소멸) — "학습은 17종, 추론 병합으로 5칸 시연" 구조. 9클래스 학습은 중복이라 생략
  - **Run B-17 학습 시작** — 1차 시도 OOM(컨테이너 RAM 한도 50GB, worker 24 과다) → worker 8로 재시작, 102,271장/60ep
  - **docs/ 폴더 신설·문서 대정리**: 설계서 v0.2 갱신(폐기된 6종 체계 등 구버전 내용 수정), 학습 보고서+그래프 작성, 인덱스·문서 규칙 수립, 본 진행일지 신설
  - 골판지 프로토타입 계획(서보2+DC1로 3분류 데모), 3D 모델링 방침(OpenSCAD 코드 CAD) 수립
- **결정**: E안 17클래스 최종 채택, 문서는 docs/에만 작성·구버전은 수정(중복 금지)
- **산출물**: `docs/` 전체 구조, `docs/03_학습_보고서.md`, 변환기 모드 17
- (오후) 웹캠 실물 테스트 환경 구축(`inference/watch_and_predict.py`, 촬영→즉시판정) —
  스프라이트병→플라스틱 오분류로 도메인 갭 실증. 복수 물체 카운트 동작 확인
- (오후) 3D 모델 v0.2→v1.0 재설계: 수직 낙하+캐러셀 정렬 방식 확정(깔때기 기각),
  과설계 제거(서보1 트랩도어·게이트/리턴 삭제), MDF 판재 구조, 검증 echo 내장(화각·낙하고·2L수용)
- **Run B-17 완료** (60ep, 총 ~7h·OOM 2회 극복): 공식 Validation 17클래스 mAP50 0.912.
  confusion matrix 분석 → 오류의 대부분이 "같은 품목 클린↔오염 혼동", 품목 병합 시 Run A급.
  **채택: Run B-17 단일 모델 + 품목병합 분배 + 오염 임계값 후처리.** 모델 회수·팟 종료(잔여 0)
- **비용 실적**: 전체 학습 캠페인 누적 약 $10 (예산 $10~20 내 완료)
- (저녁) 추론 데모를 Run B-17 + 채택 후처리(품목병합·오염임계·배출코칭)로 업그레이드 —
  Run A가 오인했던 종이컵을 정확 인식하며 복수 투입 판정 성공
- (저녁) **3D 모델 심층 개선 세션**: WSL 헤드리스 OpenSCAD 구축(AppImage) → AI가 렌더를
  직접 보며 평가-개선 3사이클. 렌더 검증으로 실결함 2건 발견·수정:
  ① 외닫이 트랩도어 개방 궤적이 수거함 관통(→ 양문 필수로 번복, 검증4 추가)
  ② 유리 낙하고 30mm vs 문짝 스윙 106mm 상충(→ 스테이지 +120mm로 양립)
  깔때기 목을 문열림 궤적 포함하게 재설계. 코드 내장 검증 4종 전부 통과.
  외장(디스플레이·투입구 베젤·회수도어·받침발)·기구 사실화(지지롤러·GT2벨트·경첩) 추가.
  렌더 3종 docs/img/cad_v11_*.png 보관
- (밤) **웹캠 실물 테스트 세션 2차** (사용자 직접, 12장, 검은 천 배경 채택): 투명 트레이→플라스틱 80% 정답,
  키친타올→종이 76% 배출 코칭 성공. 잘못된 수거함 분배 0건(애매 케이스 전원 판별불가행)

## 2026-07-19 (토)

- **한 일**: 분류테스트 12장 결과·원인 분석
  - 오류 3유형 확인: ① 검은 천 위 유령 검출(빈 배경에 glass 75% — 프레임 가장자리·어두운 주름 영역) ② 깨끗한 세제통→plastic_dirty 85%(그래픽 라벨을 오염으로 오인) + 투명 펌프캡→battery 59%(소형 원통 형태) ③ 물티슈(비닐)→paper 67%(연포장 인쇄면이 종이와 유사)
  - 근본 원인 3계층으로 정리: 도메인 갭(조명·블러·배경) / 경량 모델의 형태·색 프라이어 의존 / 후처리에 박스 위치·크기 검증 부재(유령 1개가 복수투입 오판으로 승격)
- (밤) 회색 무광 배경판으로 교체 후 추가 테스트 — 유령 박스 0건(배경 개선 효과 확인).
  단 **미학습 물체(OOD) 고신뢰 오분류 발견**: 딱풀→paper 75%(무광 원통+인쇄 라벨 프라이어),
  접착제 병→can 92%(스프레이캔 실루엣). 폐쇄형 검출기의 구조적 한계로 CONF_TH 안전망 무력화 —
  테스트 통틀어 첫 "잘못된 분배" 사례. 물티슈 팩도 paper 94%로 재오분류(검은 천 67%→회색 94%) —
  **환경 개선은 유령 검출은 제거하지만 학습 분포 밖 물체는 "더 확신 있게" 틀리게 함** →
  남은 오류는 환경이 아니라 데이터(fine-tuning)로만 해결 가능함을 실증
- **결정**: fine-tuning 촬영 목록에 그래픽 라벨 용기·물티슈류 연포장·소형 캡 필수 포함 + **비대상 물체(문구류 등)를 라벨 없는 네거티브 샘플로 추가**. 시연 물품은 학습 도메인(재활용 배출품)으로 구성. 데모 스크립트에 ROI+최소 박스 크기 필터 추가 예정
- **산출물**: 분류테스트/결과/ 판독 14건, `docs/03_학습_보고서.md` 실물 테스트 절 추가

## 2026-07-27 (일)

- **한 일**: **발표자료 3차 수정본 작성** (HRD서류 폴더, 원본과 별도 저장) — 성과 강조형에서 **자체 평가형으로 전면 개편**.
  - 최종 목표를 "싱귤레이터(1개씩 분리) → 엣지 AI 판별 → 회전판 정렬 → 투입구 개방" 4단계로 명시하고,
    현재 구현 범위는 **3종 분류(서보 한계) + 투입구 카메라 엣지 분석까지**임을 개요부터 밝힘
  - 한계점 슬라이드 → **"자체 피드백 및 후속 과제"**로 개편: 기획 단계 하드웨어 선택 미흡(SG90 토크·가동각 미검토
    → 3칸 제한, 싱귤레이터 미착수, 골판지 수작업 개입) vs 유효했던 선택(17종 학습·병합, 파인튜닝+사전 게이트,
    엣지 TensorRT, 저비용 센서) 구분 + 후속 과제(싱귤레이터 설계 / 구동계 재선정 / MDF·3D프린트 재질 개선) 1:1 대응
  - **거짓 없는 수치·서사로 교정**: 실제 제작에 활용 못한 OpenSCAD CAD 강조 제거, 추론 속도를 채택 모델 기준
    51.5ms로 갱신, ft1 "게이트 0.892 미달(0.880) → 보류 → 실물 7/7 재검증 후 채택" 2단계 의사결정 명시
- **결정**: 발표 톤 = 좋았던 점/아쉬운 점/발전 방향 균형 서술, 성과 과시·미활용 작업 강조 배제
- **Wokwi 회로도 최종 설계 반영**: 구버전(`pico_sorter/wokwi`, 서보2개 4칸 v1)을 최종 펌웨어 위치
  `pico_sonar_test/wokwi/`로 이전·전면 갱신 — 서보1 도어 GP0(90/180)·서보2 회전판 GP1(0/90/180 3칸),
  HC-SR04 GP16/17(1k/2k 분압), LCD1602 I2C GP4/5, **서보 전원 별도(VCC 심볼)+GND 공통** 반영.
  main.py도 실물 C 상태머신(ARMED→SETTLING 3s→DETECT→WAITCMD→COOLDOWN)과 동일하게 재작성
- **프로젝트 기술서 작성**: 2차 기술서(hwp) 양식 구조를 그대로 재현해 최종 프로젝트 기술서 docx 생성
  (작성자 전제윤, 팀원 김도경·박시영 역할 분담 기재. 수행목표에 최종 4단계 목표 vs 현재 구현 범위 구분,
  상세 내용 5절: 본학습/파인튜닝·게이트/엣지 추론/기구 제어/한계·후속과제, 이미지 7종 삽입 — 회로도·학습곡선·혼동행렬·실물판정·시연)
- **발표문 작성 + 배경 서사 개편**: 슬라이드 1~11 발표문(약 10분) 작성. 이후 2·3번 슬라이드와 발표문을
  **편의성 중심 서사**(사람이 분류하지 않는 시스템을 아파트 단지 재활용장 단위로)로 재구성
- **산출물**: `발표자료_3차 수정본.pptx`, `발표문_1조_분리수거_자동분류기.docx`, `최종_1조_프로젝트_기술서_분리수거_자동분류기.docx` (HRD서류 폴더 — repo 외부),
  `pico_sonar_test/wokwi/{diagram.json,main.py}`

## 2026-07-24 (목)

- **한 일**:
  - **ft1 Jetson 배포 완료**: ONNX(opset12) 변환 → 전송 → TensorRT FP16 엔진 빌드(`runB17_ft1.engine`,
    추론 **51.5ms/프레임** — 기존 49.6ms와 동급) → `jetson_infer.py` 엔진 전환 → `sorter.service` 재시작.
    기동 확인: 엔진 로드·시리얼(/dev/ttyACM0)·웹모니터(8081) 정상. 기존 `runB17.engine`은 롤백용 보존
  - **웹모니터 영상 미표시 트러블슈팅**: 원인은 카메라가 아니라 **기동 순서** — 브리지 시작 시점에
    `/dev/video0` 미존재(uvcvideo가 13분 뒤 등록, dmesg로 확인) → 카메라 열기 1회 실패 후 그대로 운용,
    감지 시 프레임 0장으로 무조건 REJECT. 재시작으로 즉시 해결 + **자동 복구 패치**(열기 재시도 30회,
    운용 중 6초 연속 실패 시 재연결, 최종 실패 시 종료→systemd 재기동)
  - **ft1 실물 검증 7/7 전원 정답** (`docs/img/ft1_test_*.png`, 신뢰도 94~99%):
    종이뭉치 paper 98 / 종이컵 paper_pack 99 / **본드병 plastic 97(기존 can 92% 오분류 해결)** /
    돔뚜껑 pet 98 / 찌그러진 페트병 pet 94 / **투명 지퍼백 vinyl 97(기존 전 프레임 미검출 해결)** /
    검정 비닐 vinyl 98 — 자체 val 0.995가 실물에서 재현됨
- **결정**: **ft1 채택 확정** — 파인튜닝 목적(OOD 물체·투명 비닐 사각지대)이 실물로 입증됨.
  일반화 −3.2%p는 시연 범위에서 무영향
- **산출물**: `models/runB17_ft1_best.onnx`, Jetson `runB17_ft1.engine`, `inference/jetson_infer.py`(엔진 전환),
  `inference/jetson_bridge.py`(카메라 자동 복구), `docs/img/ft1_test_*` 7종

## 2026-07-22 (화) ~ 07-23 (수)

- **한 일**:
  - **골판지 프로토타입 기구 조립·캘리브레이션**: 투입구=서보1(물리핀1, 닫힘90°/열림180° 실측),
    회전판=서보2(물리핀2). FSR 검토 후 초음파로 회귀. 십자 프레임(팔 90° 간격) 제작
  - **3칸 분배 체계 확정**: 0°=종이(+종이팩) / 90°=플라스틱(+투명페트) / 180°=비닐, 그 외 REJECT 리턴.
    십자 프레임과 SG90 180° 한계에 정합 (SG90은 270° 불가 — 소프트웨어 아닌 내부 기구 한계)
  - **펌웨어 상태머신 완성** (`pico_sonar_test`): 감지→3초 안정화→DETECT→분석대기(초음파 무시)→
    SORT(회전판→문 개폐)→쿨다운→재무장. LCD 상태 표시 전체(READY/DETECTED/ANALYZING/RESULT/COMPLETE/리턴사유별)
  - **10프레임 다수결 판정 도입**: 1초간 10프레임 → 프레임별 판정 → 최다 득표 채택 (시간축 앙상블)
  - **노트북 브리지** (`bridge.py`, 8092): 브라우저 한 페이지에서 웹캠+Web Serial(COM5)+판정 —
    실시간 미리보기·프레임별 오버레이·감지 로그·C920 자동 선택
  - **fine-tuning 수집 도구** (`collect_data.py`, 8091): 숫자키=클래스, 위치는 모델 프리라벨(클래스무관
    최고신뢰 박스)·클래스는 사람 — 반자동 라벨링. 오버레이 미리보기·x키 실행취소
  - **Jetson 전자동 브리지 완성·검증** (`jetson_bridge.py`): DETECT→10프레임(GPU, 1초!)→다수결→SORT/REJECT.
    노트북 모니터링 웹(8081, MJPEG+로그) 내장. **REJECT 경로 실기 검증** (투명 지퍼백 → 전 프레임 미검출 →
    리턴 — 안전망 정상. 지퍼백은 비닐 fine-tuning 목록에 추가)
  - **모델 동일성 3중 검증**: ONNX md5 일치 / 17클래스 매핑 완전 동일 / 동일 이미지 교차추론 (can 0.479 vs 0.441,
    fp16 오차 정상 범위)
  - **systemd 자동 시작 등록·재부팅 실증**: 전원만 꽂으면 ~40초 후 분류기 자동 가동 (sorter.service, Restart=always)
  - 트러블슈팅: Jetson 시리얼 권한(dialout 그룹), 원격 pkill 자기종료 함정(브래킷 패턴), 발열 실측(부하 중 GPU 39°C)
  - **운용 조정**: 신뢰도 리턴 기준 0.50→**0.40** 완화(4개 판정 코드 일괄, 리턴율 개선 목적).
    LCD 개선 2건 — 대기화면에 **마지막 판정 결과 유지**(`LAST: PAPER`), 서보 노이즈로 인한 글자 깨짐에
    **자가 복구**(사이클 복귀·30초마다 재초기화). **Jetson에서 Pico 직접 굽기** 확립(부트로더 드라이브 원격 마운트 —
    케이블 이동 불필요). 모델 동일성 3중 검증(ONNX md5·매핑·교차추론), 노트북 분리 자립 운용 확인
  - **시연 영상 4편 확보** (`docs/img/demo_cycle_*.mp4`, 총 38초): 조립 완성체에서
    지퍼백·투명 컵뚜껑 투입 → LCD 상태 전환(DETECTED→ANALYZING) → 사이클 진행.
    PPT용 스틸컷 2장 추출(`demo_still_lcd_*.jpg`)
  - **시연용 fine-tuning 1차(ft1) 완료** (RunPod A40, 학습 ~10분):
    자체 수집 45장(시연 물체 5클래스, 반자동 라벨링)×15 오버샘플 525장 + AI Hub 서브샘플 10,952장,
    15에폭, lr0=0.0005(본학습의 1/10). numpy 2.x 비호환 크래시 1회(`pip install "numpy<2"`로 해결)
  - **ft1 평가 — 시연 특화 확인, 일반 성능은 소폭 하락**:
    자체 val(시연 물체 10장) mAP50 **0.995** / 공식 3,000장 게이트 mAP50 **0.880**
    (기존 0.912 대비 −3.2%p, 합격 기준 −2%p 미달). 하락 주도는 plastic(0.783)·오염 소수 클래스.
    시연 핵심(종이 0.962/비닐 0.913/투명페트 0.898)은 유지 → **시연 한정 배포 후보**로 보관
  - **모델·결과 회수 및 팟 종료**: `models/runB17_ft1_best.pt` 다운로드,
    학습 곡선·혼동행렬 `training/results_ft1/` + `docs/img/ft1_*` 보관, 팟 종료 확인(잔여 0)
- **결정**: 최종 구성 = Jetson(자동시작 브리지) + Pico(상태머신) + 노트북(모니터링만, 분리 가능).
  ft1은 게이트 미달로 **기본 채택 보류** — Jetson 배포 여부는 실물 재검증 후 결정(시연 물체 정확도 vs 일반화 트레이드오프)
- **산출물**: `pico_sonar_test/`(최종 펌웨어), `inference/bridge.py·collect_data.py·jetson_bridge.py`,
  Jetson `sorter.service`, `docs/06` v3 갱신, `models/runB17_ft1_best.pt`, `training/results_ft1/`,
  `docs/img/ft1_results_curves.png·ft1_confusion_matrix.png`

## 2026-07-21 (월)

- **한 일**:
  - **한 물체 다중 판정 버그 수정**: 스프라이트병에 `pet_transparent`·`plastic` 박스가 동시 생성.
    원인은 **NMS가 클래스별로 수행**되어 서로 다른 클래스 박스가 상호 제거되지 않은 것 →
    IoU 0.55 기준 **군집화 후 군집당 최고 신뢰도 1개만 대표**로 남기는 로직 추가.
    Jetson판·노트북판 양쪽 반영. 방치 시 "복수 투입" 오판을 유발하는 문제였음(세제통 사례와 동일 구조)
  - **일정상 범위 축소 결정 반영**: 싱귤레이터(클리트 컨베이어) 구현 제외,
    분류 4칸(품목 3종 + 기타)으로 축소, **오염 판정 보류**(오염 클래스를 같은 품목 클린으로 간주)
    → `jetson_infer.py` 상단에 `IGNORE_DIRTY`, `BIN_ITEMS` 설정으로 구현. **재학습 불필요**
- **결정**: 축소 운용안 채택 — 모델은 17종 그대로 두고 판정 계층만 단순화(설계 의도대로 활용).
  분배 품목 기본값 `pet / can / plastic`, 그 외 전부 기타함
- **산출물**: `inference/jetson_infer.py`(군집화+축소 설정), `inference/watch_and_predict.py`(군집화)

## 2026-07-20 (일)

- **한 일**:
  - 로컬 데이터셋 zip 40개 전량 해제·검증·삭제 (Training 해제본 19GB만 유지, Validation은 val_indoor와 중복 확인 후 zip 제거)
  - **HC-SR04 투입 감지 테스트 구축**: Pico 2 W C 펌웨어(`hcsr04_test/`, 3회 중앙값·히스테리시스·DETECT 이벤트) 빌드, Wokwi 시뮬레이션 파일 작성, 분압(1k/2k) 실배선
  - **원격 배선 진단 체계 확립**: PowerShell 경유로 노트북에서 COM5 시리얼 직접 읽기 + 부트로더 드라이브에 uf2 원격 굽기 성공. 진단 펌웨어(diag.c)로 판정 — 배선 전체 정상, "초음파 반사 미회수"만 남음(센서 방향 또는 VCC 확인 필요, 미해결)
  - **엣지 보드 확정: Jetson Nano Developer Kit** (보유 장비 활용, 구매비 0원) — Pi Zero 2 W 구매 계획 폐기. TensorRT fp16로 추론 0.03~0.05초 예상, C920 웹캠 USB 직결(도메인 일치 유지)
  - 최종 하드웨어 구성 확정: Jetson(촬영·추론·판정) + Pico 2 W(HC-SR04·SG90×2·LCD1602, USB 시리얼) + 서보 전원 분리(AA×4 또는 5V 어댑터)
  - Nano 배포 준비: **ONNX 변환 완료** (`models/runB17_yolo11n_17cls_best.onnx`, opset12 — JetPack 4.6 TensorRT 호환)
  - (밤) **Pico 하드웨어 3종 통합 검증 완료**:
    - 초음파 — 접촉 불량(점퍼 절연테이프 이음)이 원인이었음. 재배선 후 3cm~259cm 안정 측정
    - LCD1602 — I2C 무응답 → 4핀 재연결로 통신 성공 → 글자 깨짐 → **원인은 매핑이 아닌 초기화 타이밍 부족**
      (진단 펌웨어 `lcd_test.c`로 MAP A/B 6초 교대 표시하여 판별). 150us/6ms로 여유 확보 후 정상. 주소 0x27
    - 서보 2개 — 건전지 홀더(AA×4, 6V) 별도 레일 + GND 공통 배선 확립. `all` 명령으로 4종 일괄 점검 통과
    - 진단 도구 4종 작성: `diag.c`(초음파 3단) `i2cfind.c`(전핀 I2C) `pinfind.c`(외부 풀업) `lcd_test.c`(매핑)
  - **Jetson Nano 셋업 완료 (무모니터 원격)**: JetPack 4.6.1 굽기 → USB 시리얼 콘솔(COM6)로 초기 설정 전 과정
    원격 진행(라이선스·언어·계정 jetson/jetson1234·MAXN·DHCP) → SSH 키 주입 → 비밀번호 없는 원격 접속 확립.
    호스트명 jetson-nano, 시간대 Asia/Seoul, 랜선 인터넷 연결
  - **TensorRT FP16 엔진 변환 성공**: ONNX(11MB) → engine(9.3MB), **실측 49.6ms/프레임(초당 20장)**.
    노트북 CPU 대비 6~10배. C920 웹캠도 `/dev/video0`으로 인식 확인
  - **포트폴리오 체계 수립**: `docs/05_포트폴리오_기록.md` 신설(상세 서술형), `docs/img/`에 하드웨어·테스트·성능 이미지 축적 시작
- **결정**: 엣지 = Jetson Nano + TensorRT(확정, 실측 검증), 카메라 = C920 유지(fine-tuning 도메인 일관성),
  Pico는 센서·구동 전담, 전원 = 보드/서보 분리 원칙, **포트폴리오 자료는 상시 축적**(진행일지+상세기록+이미지 3종)
- **산출물**: `hcsr04_test/`, `pico_hw_test/`(진단 4종+통합테스트+sonar_lcd), `models/*.onnx`,
  Jetson `~/runB17.engine`, `docs/04_하드웨어_배선.md`, `docs/05_포트폴리오_기록.md`, `docs/img/` 8종 추가
